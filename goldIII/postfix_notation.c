#include <stdio.h>

void	block_recur(char *buf);

static char		*buf_ptr;

int	main(void)
{
	char	buf[102] = {0, };
	char	ret[102] = {0, };
	char	*ret_ptr = ret;
	int		flag;

	buf_ptr = buf;
	fgets(stdin, buf, 102);
	
}
void	block_recur(char *buf);
{
	
}
